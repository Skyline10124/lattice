use anyhow::Result;
use colored::Colorize;

pub fn run() -> Result<()> {
    println!("{}", "Session statistics".bold());
    println!("(Statistics tracking not yet implemented in MVP)");
    Ok(())
}
