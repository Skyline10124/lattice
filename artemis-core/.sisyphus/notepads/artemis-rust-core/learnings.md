
## 2026-04-28: v0.1.0 Release (T40)

- Cargo.toml and pyproject.toml both already at version `0.1.0` — no version bump needed.
- All 380 tests pass (273 lib + 27 e2e + 38 provider + 42 transport) with 0 failures.
- Created annotated tag `v0.1.0` on HEAD (`622f700`).
- Tag message: `release: v0.1.0`.
- Did NOT push to remote or publish to crates.io/PyPI.
